package com.example.pokedex.utilities;

/**
 * Enum that defines different output formats for displaying Pokemon data.
 * This allows the user to choose between text, HTML, and CSV representations.
 */
public enum OutputFormat {
    TEXT, HTML, CSV
}
